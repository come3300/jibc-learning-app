{/* http://localhost:3000/documents/phase1/linux/about-linux */}

<div class="bg-white">
    <div class="whitespace-nowrap mx-auto max-w-screen-md ">
    ## Linuxとは

    ***

     <img src="/phase1/website/hello-world/helloworld.png" loading="lazy" alt="Photo by Minh Pham" class="" />
    macOSやWindwosと同じOSの1つである。
    Webサーバーを作る際に必須となる。

    ## OSとは

    <img src="/phase1/linux/OS.png" loading="lazy" alt="Photo by Minh Pham" class="" />

    Operating Systemの略称。
    パソコンを動かすためにはこのOSが必ず必要になります。

    皆さんが普段使っているPCにも必ずOSが入っています。
    MacBookにはMacOS,WindowsPCにはWindowsといった具合にOSが導入されています。


    ### Linuxの特徴

    <img src="/phase1/linux/linux_logo.png" loading="lazy" alt="Photo by Minh Pham" class="" />

    ・CUI操作を基本としている
    ・無料で利用できる

    #### CUI操作を基本としている


    Linuxは、テキストベースのコマンドラインインターフェース（CUI）を基本として操作されます。
    これは、ユーザーがコマンドを入力してターミナル上で操作を行うことを意味します。

    例えばCUIを使ってsampleという名のディレクトリを作成したい時には
    下記のコマンドを入力します。

    ```bash
    $ mkdir sample
    ```

    このように入力することでsampleディレクトリが作成されます。
    ここはもっと生徒が操作して実感できるようにしていきたい。

    ### 無料で利用できる

    Linuxは基本的に無料で利用可能です。

    例えば、Windows11のOSを入れているPCでLinuxを使用したいときは
    下記コマンドを入力します。

    ```bash
    $ wsl --install
    ```
    このコマンド一つでLinuxの導入が無料でできてしまいます。

    他にも中古のPCを購入してその中にLinuxを導入してサーバーとして
    利用するなんて方法もできてしまいます。

    </div>

</div>
